version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - yarn
  test:
    commands: 
      - yarn test
artifacts:
  files:
    - 'lint-report.html'
  name: test/$CODEBUILD_BUILD_NUMBER/my-report